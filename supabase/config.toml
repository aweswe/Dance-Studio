# Supabase CLI config — see https://supabase.com/docs/guides/cli/config
# Links this directory to the production project so `supabase db push`
# targets the right remote when a personal access token is present.
project_id = "pndbazmwnbqzkwwmmgaf"

[auth]
site_url = "http://localhost:3000"
additional_redirect_urls = [
  "http://localhost:3000/**",
  "http://localhost:3000/api/auth/callback",
  "http://127.0.0.1:3000/api/auth/callback",
  "https://rhythmzz.in/**",
  "https://rhythmzz.in/api/auth/callback",
]
